<h1>New comment</h1>

<%=form_for :comment, :url => {:controller => "comments", :action => "create"} do |f| %>

 

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  
   <p><%= f.submit %></p>
  
 
<% end %>


<%= link_to 'Back', comments_path %>
