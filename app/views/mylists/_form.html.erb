<%= simple_form_for(@mylist) do |f| %>
  <% if @mylist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mylist.errors.count, "error") %> prohibited this mylist from being saved:</h2>

      <ul>
      <% @mylist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
