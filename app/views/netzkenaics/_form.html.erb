<%= simple_form_for(@netzkenaic) do |f| %>
  <% if @netzkenaic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@netzkenaic.errors.count, "error") %> prohibited this netzkenaic from being saved:</h2>

      <ul>
      <% @netzkenaic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
