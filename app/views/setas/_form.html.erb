<%= simple_form_for(@seta) do |f| %>
  <% if @seta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seta.errors.count, "error") %> prohibited this seta from being saved:</h2>

      <ul>
      <% @seta.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
