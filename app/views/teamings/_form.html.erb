<%= simple_form_for(@teaming) do |f| %>
  <% if @teaming.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teaming.errors.count, "error") %> prohibited this teaming from being saved:</h2>

      <ul>
      <% @teaming.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
