<%= simple_form_for(@naic) do |f| %>
  <% if @naic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@naic.errors.count, "error") %> prohibited this naic from being saved:</h2>

      <ul>
      <% @naic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
