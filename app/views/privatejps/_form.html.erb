<%= simple_form_for(@privatejp) do |f| %>
  <% if @privatejp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@privatejp.errors.count, "error") %> prohibited this privatejp from being saved:</h2>

      <ul>
      <% @privatejp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
