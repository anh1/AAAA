<%= simple_form_for(@obv) do |f| %>
  <% if @obv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@obv.errors.count, "error") %> prohibited this obv from being saved:</h2>

      <ul>
      <% @obv.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
